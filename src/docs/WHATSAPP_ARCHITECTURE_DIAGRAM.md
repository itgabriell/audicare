# 🏗️ WhatsApp Integration: Architecture & Data Flow

**System:** AudiCare Inbox Module

## 1. High-Level Architecture

The system uses a **Client-Side Polling** architecture to interface with the **AudiCare API Gateway**, which wraps the underlying WhatsApp provider (Uazapi/Evolution).